# 合并到 Nginx 主配置「http { }」内、与具体 server 平级，整站只需一份。
# 与 nginx-api2.example.conf 中 location ^~ /api/v1/stream/ 的 zone=short_stream 对应。
limit_req_zone $binary_remote_addr zone=short_stream:16m rate=20r/s;
